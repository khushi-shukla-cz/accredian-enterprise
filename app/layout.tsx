import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Navbar from '@/components/Navbar'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Accredian Enterprise - Transform Your Workforce with AI & Data Science',
  description: 'Enterprise learning solutions for AI, ML, and Data Science. Upskill your teams with industry-focused programs.',
  keywords: 'enterprise learning, AI training, data science, machine learning, corporate training',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  )
}
